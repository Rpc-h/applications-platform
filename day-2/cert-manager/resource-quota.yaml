apiVersion: v1
kind: ResourceQuota
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  hard:
    requests.cpu: "100m"
    requests.memory: 256Mi
    requests.storage: 10Gi
    limits.cpu: "100m"
    limits.memory: 256Mi