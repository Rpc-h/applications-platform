apiVersion: v1
kind: ResourceQuota
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  hard:
    requests.cpu: "100m"
    requests.memory: 256Mi
    requests.storage: 10Gi
    limits.cpu: "200m"
    limits.memory: 512Mi